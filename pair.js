import { makeWASocket, useMultiFileAuthState } from '@whiskeysockets/baileys';
import express from 'express';

const router = express.Router();

// Your pair code here using import instead of require

export default router;
